Since it is a simple 4-tap one, I've used separate ports for inputs real and imaginary parts.  
So, their is no need to make correct order again for outputs, since they are available at correct mentioned ports right.  

Latency: 2 Clocks
